import 'package:flutter/material.dart';

class ExtendedSidebarConstants {
  final double headerHeight = 60;
  Color get headerColor => const Color.fromRGBO(3, 102, 102, 0.6);
  Color get containerColor => const Color.fromRGBO(3, 102, 102, 0.9);
}
