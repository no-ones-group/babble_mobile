import 'package:flutter/material.dart';

class SpaceRootConstants {
  final double headerHeight = 60;
  Color get headerColor => const Color.fromRGBO(3, 102, 102, 0.7);
  Color get containerColor => const Color.fromRGBO(3, 102, 102, 1);
}
